import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
  grid: {
    margin: 'auto'
  },
  btnFilter:{
    float: 'right',
    paddingLeft: '10px'
  },
  warningMsg:{
    color: '#cf391d'
  }
})

export default styles
