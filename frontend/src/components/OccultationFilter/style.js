import { makeStyles } from '@mui/styles';

const styles = makeStyles({
  grid: {
    margin: 'auto'
  },
  btnFilter: {
    float: 'right',
    paddingLeft: '10px'
  },
  warningMsg: {
    color: '#cf391d'
  }
})

export default styles
