import { makeStyles } from '@mui/styles'

const useStyles = makeStyles((theme) => ({
  labelInfo: {
    fontSize: 12
  },
  labelTimes: {
    fontSize: 12,
    textAlign: 'right',
    marginRight: '50px',
    marginTop: '-20px',
    display: 'block'
  }
}))

export default useStyles
