import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
  particlesWrapper: {
    position: 'absolute',
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
    zIndex: 1,
  },
});

export default styles;
