import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
  particlesWrapper: {
    position: 'absolute',
    bottom: 0,
    top: 0,
    zIndex: 1,
    marginTop: '65px',
    width: '100vw',
    height: '41vh',
  },
});

export default styles;
