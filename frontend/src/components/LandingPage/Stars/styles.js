import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
  particlesWrapper: {
    position: 'absolute',
    bottom: 0,
    top: 64,
    zIndex: 1,
    width: '100%',
    height: 380,
  },
});

export default styles;
