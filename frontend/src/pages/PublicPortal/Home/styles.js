import { makeStyles } from '@material-ui/core/styles'

const styles = makeStyles({
  root: {
    padding: '0 5%',
    paddingBottom: '55px',
    background: '#e2e1e2',
  },
  mainInterfaces: {
    backgroundColor: 'red'
  }
})

export default styles
