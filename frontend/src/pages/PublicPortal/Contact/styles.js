import { makeStyles } from '@material-ui/core/styles'

const styles = makeStyles(() => ({
  initContainer: {
    paddingTop: 20,
    paddingBottom: '60vh',
  },
  grid: {
    margin: 'auto',
    marginTop: 20,
  }
}))

export default styles
