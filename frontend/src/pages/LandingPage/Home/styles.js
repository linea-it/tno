import { makeStyles } from '@mui/styles'

const styles = makeStyles({
  root: {
    padding: '0 5%',
    paddingBottom: '55px'
  },
  mainInterfaces: {
    backgroundColor: 'red'
  }
})

export default styles
