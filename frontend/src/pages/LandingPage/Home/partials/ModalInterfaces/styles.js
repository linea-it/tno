import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles(() => ({
  action: {
    color: 'darkslateblue',
    fontSize: '0.9em',
    padding: '0px 8px',
    zIndex: 9999,
  },
  dialog: {

  },
}));

export default styles;
