import { makeStyles } from '@mui/styles'

const styles = makeStyles(() => ({
  action: {
    color: 'darkslateblue',
    fontSize: '0.9em',
    padding: '0px 8px',
    zIndex: 9999
  }
}))

export default styles
